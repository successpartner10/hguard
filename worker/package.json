{
  "name": "ai-home-guard-worker",
  "private": true,
  "version": "0.1.0",
  "description": "AI Home Guard \u2014 Cloudflare Workers + Durable Objects backend (Phase 6).",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}